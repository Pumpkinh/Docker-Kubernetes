apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "course-app.fullname" . }}-config
  labels:
    {{- include "course-app.labels" . | nindent 4 }}
data:
  APP_BANNER: {{ .Values.config.appBanner | quote }}
  APP_VERSION: {{ .Values.config.appVersion | quote }}
